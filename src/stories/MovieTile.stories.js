import { MovieTile } from '../Components/MovieTileComponent/MovieTile';

export default {
    title: 'Extra / MovieTile Component',
    component: MovieTile,
}

const Template = () => <MovieTile/>;

export const Default = Template.bind({});